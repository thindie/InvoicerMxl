package com.thindie.invoicer.application.update

/**
 * Must match the distributed app version (e.g. [org.gradle.api.Project.getVersion] / Compose packageVersion).
 */
object AppVersion {
  const val SEMVER: String = "2.0.5"
}
